{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "keycrypt.serviceAccountName" . }}
  labels:
    {{- include "keycrypt.labels" . | nindent 4 }}
{{- end }}
